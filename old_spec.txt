1-TOPS Submission Document 1.0 TOC \h \u \z \t &quot;Heading 1,1,Heading 2,2,Heading 3,3,Heading 4,4,Heading 5,5,Heading 6,6,&quot;Function of the SoC2High-level Block Diagram3Interface pins5Clocks6Key modules6CPU &amp; Instructions Used6FFT Block7Memory Modules7Work in progress/Things to be figured out:8Function of the SoCThe proposed System-on-Chip (SoC) is designed to selectively filter specific sounds from real-time audio based on reference audio samples recorded by the user. The primary application is in assistive headphones for people on the autism spectrum, who may find certain individual-specific sounds triggering. Instead of conventional active noise cancellation, which suppresses all environmental sound, this system allows users to suppress only personalised, selected sounds while allowing the rest of the auditory environment to remain audible.The filtering is achieved by comparing the spectral characteristics of incoming real-time audio with the spectral characteristics of previously recorded trigger sounds.The SoC operates in two modes: record mode and real-time mode, which can be toggled using a GPIO push button. In record mode, the user records fixed-length audio samples of sounds they wish to suppress using a microphone. These samples are processed to learn characteristic spectral patterns of the trigger sound. In real-time mode, the SoC continuously captures environmental audio through the microphone, processes it, and outputs filtered audio to a DAC connected to headphones.In both modes, audio input is received through a microphone that transmits PCM samples via an I²S interface. The incoming samples are collected into frames of 256 samples, where successive frames overlap by 50%, and passed to an FFT accelerator for spectral analysis. The FFT output is then processed by the CPU. After the required processing in the spectral domain, the modified frequency components are forwarded to the IFFT block for time-domain reconstruction of the audio signal. The reconstructed samples are then transmitted through the I²S interface to the DAC, which converts the digital signal into an analog signal that is finally played through the speaker.In record mode, the recorded audio is used to compute feature vectors representing the characteristic spectral directions of the trigger sound. These vectors are learned using Oja’s rule, an online variant of Principal Component Analysis (PCA). As successive frames are processed, the vectors gradually align with the principal spectral directions of the trigger sound subspace, representing the dominant spectral patterns of the sound. The learned trigger vectors are then stored in flash memory for use during real-time filtering.In real-time mode, each incoming frame’s spectrum is compared against the learned trigger subspace. If the similarity score exceeds a predefined threshold, the frequency bins corresponding to the trigger sound’s learned trigger patterns are attenuated. The modified spectrum is then transformed back to the time domain using an inverse FFT, and overlap-add reconstruction is used to produce the filtered audio signal.High-level Block Diagram-485774419100-4381491133475Interface pinsInterface pins of the SoC are listed below:Port nameDirectionFunctionVoltage (V)core_clkInputPrimary clock source1.8rst_nInputSoC reset1.8rx_i2s_sckOutputSerial clock to MIC ADC1.8rx_i2s_sdInputSerial data from MIC ADC1.8rx_i2s_wsOutputSerial Word select to MIC ADC1.8tx_i2s_sckOutputSerial clock to Speaker DAC1.8tx_i2s_sdOutputSerial data to Speaker DAC1.8tx_i2s_wsOutputSerial Word to Speaker DAC1.8rec_realInput1’b1 - record mode to receive the trigger pattern1’b0 (default) - real time filter mode1.8clr_recInputClears the recorded trigger patterns (sense the rise edge for clear)1’b0 (default)1.8spi_sckOutputSPI clock1.8spi_mosiOutputSPI Master out Slave in1.8spi_misoInputSPI Master in Slave out1.8spi_csOutputSPI chip select1.8tckInputJTAG Clock1.8tmsInputJTAG mode select1.8tdiInputJTAG Data in1.8tdoOutputJTAG Data out1.8trst_nInputJTAG reset1.8func_testInputJTAG compliance pin to choose debug or test/dft1’b0 - func1’b1 - TestGpio (tbd)(tbd)…(tbd)(tbd)ClocksMain clock domains of the SoC are described below:Clock nameClock frequencySourcecore_clk150MHz - 200MHzExternal source - through clock IOi2s_lrclk15KHzDivided from core_clki2s_blclk1MHzDivided from core_clki2s_mclk3.84MHzTBD (external source!)spi_clk25MHzDivided from core_clkapb_clk75MHz - 100MHz1:2 of core_clkahb_clk150MHz - 200MHz1:1 of core_clkKey modulesCPU &amp; Instructions UsedISA: RV32I processor with the M extension Pipeline: 1–3 stage in-orderPrivilege: M-modeMemory: 8–32 KB SRAM(on chip), 4 KB Boot ROMTCMDTCM16 kB to store 4 trigger vectors of 4 kB each (number of triggers which can be stored can be increased based on exactly how much area we have left at the end)10 B for coefficients and gains used while filteringITCMSize yet to be decidedInteger instructions:ADD, ADDI, SUBSLL, SLLI, SRL, SRLI, SRA, SRAILW, LH, SW, SHBEQ, BNE, BLT, BGEJAL, JALRLUI, AUIPCM extension: MUL, DIV, REMFFT BlockA 256 point FFT accelerator is used for frequency domain calculations. It performs FFT (Fast Fourier Transform) as well as IFFT (Inverse Fast Fourier Transform).It has 10 kB scratchpad memory to store the inputs and stage-by-stage output. It consists of three submodules namely Top_Module, Butterfly_Module, Twiddle_Module.Top_Module: Contains an FSM which will decide whether to Read/ Write data, calculate FFT or calculate IFFT.Keeps track of the stage which the Butterfly_Module block is executing.Butterfly_Module:Contains one instance of FFT butterfly which performs the mathematical operations of A+B*W and A-B*W where A and B are the inputs and W is the twiddle factor of that particular stageTwiddle_Module:Serves as the storage module storing the necessary values for FFT and IFFT. Memory Modules1 kB FIFO for 1 frame of input sound samples i.e. inputs of FFT 1 kB FIFO for frequency spectrum of input sound i.e. output of FFT 0.5 kB buffer for magnitude spectrum of FFT output0.5 kB buffer for phase spectrum of FFT output0.5 kB buffer for filtered magnitude spectrum of FFT output1 kB FIFO for frequency spectrum of filtered sound i.e. input of IFFT1 kB FIFO for 1 frame of input sound samples i.e. outputs of IFFT Boot ROMTotal: 11 kB + Boot ROMImplementation of each memory block as SRAM/flops will be decided based on area.Work in progress/Things to be figured outCPU microarchitectureRobust boot sequenceCurrently the filtration process does not work for some types of sounds, the logic needs to be modified and optimized more.Decide between using XIP and pulling all instructions to TCM while booting. Need to decide if there is an option to overwrite the code externally by using flash or JTAG.Finalise  memory size for on-chip SRAM, boot ROM and flash memory based on leftover area and firmware size.